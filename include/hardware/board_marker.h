#pragma once

/**
 * Searchable board identity baked into the app image for the WebFlasher.
 * Format: "FSBRDMK:" FLIGHTSCNR_BOARD_NAME  (see docs/flasher.js).
 */
extern "C" const char kFlightScnrBoardMarker[];
